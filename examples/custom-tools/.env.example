# Custom Tools Configuration
# Copy this file to .env and fill in your values
#
# For AI/ML Scientists:
# Environment variables keep sensitive data (API keys, passwords) out of code.
# Like keeping your random seeds and hyperparameters separate from model code.

# =============================================================================
# SageMaker LLM Configuration
# =============================================================================

# Real SageMaker endpoint (production)
SAGEMAKER_ENDPOINT_NAME=langgraph-mistral-7b-endpoint

# OR: Mock endpoint for local testing (no AWS costs)
# SAGEMAKER_ENDPOINT_URL=http://localhost:8080

# =============================================================================
# Database Configuration
# =============================================================================

# Database type: sqlite, postgres, or mysql
DB_TYPE=sqlite

# SQLite (file-based, no server needed - good for testing)
SQLITE_FILE=/tmp/test_sales.db

# PostgreSQL (uncomment if using)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=sales_db
# DB_USER=your_username
# DB_PASSWORD=your_password

# MySQL (uncomment if using)
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=sales_db
# DB_USER=your_username
# DB_PASSWORD=your_password

# =============================================================================
# API Keys
# =============================================================================

# Weather API (weatherapi.com)
# Get free key: https://www.weatherapi.com/signup.aspx
# Free tier: 1 million calls/month
WEATHER_API_KEY=your_weather_api_key_here

# Stock API (Alpha Vantage)
# Get free key: https://www.alphavantage.co/support/#api-key
# Free tier: 5 API calls/minute, 500 calls/day
STOCK_API_KEY=your_stock_api_key_here

# =============================================================================
# File System Configuration
# =============================================================================

# Allowed directories for file operations (security sandboxing)
ALLOWED_FILE_DIR=/tmp/safe_files
ALLOWED_WRITE_DIR=/tmp/safe_output

# =============================================================================
# Optional: Custom API Configuration
# =============================================================================

# If you're using the generic API tool template
# CUSTOM_API_BASE_URL=https://api.yourcompany.com
# CUSTOM_API_KEY=your_api_key_here

# =============================================================================
# Usage Instructions
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your actual values:
#    nano .env
#
# 3. Load environment variables:
#    # Option 1: In your shell
#    export $(cat .env | xargs)
#
#    # Option 2: In Python
#    from dotenv import load_dotenv
#    load_dotenv()
#
#    # Option 3: In Docker Compose
#    # It automatically loads .env files
#
# 4. Verify configuration:
#    python -c "import os; print(os.getenv('WEATHER_API_KEY'))"
#
# =============================================================================
# Security Best Practices
# =============================================================================
#
# - NEVER commit .env to version control
# - Add .env to .gitignore
# - Use different .env files for dev/staging/prod
# - Rotate API keys regularly
# - Use AWS Secrets Manager or similar for production
# - Limit API key permissions to minimum required
#
# =============================================================================
# Cost Warnings
# =============================================================================
#
# Free tiers (as of 2024):
# - Weather API: 1M calls/month (plenty for development)
# - Stock API: 500 calls/day (careful - can hit limits quickly)
# - SQLite: Free (local file)
# - SageMaker: ~$0.70/hour for ml.g5.xlarge (not free!)
#
# To avoid costs:
# - Use SAGEMAKER_ENDPOINT_URL with mock endpoint for local dev
# - Cache API responses during development
# - Use SQLite instead of hosted databases for testing
#
